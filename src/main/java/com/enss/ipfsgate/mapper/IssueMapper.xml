<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enss.ipfsgate.mapper.IssueMapper">
    <resultMap id="IssueResultMap" type="java.util.Map">
        <result column="u_id" property="uId" javaType="java.lang.Integer" jdbcType="INTEGER" />
        <result column="u_name" property="uName" javaType="java.lang.String" jdbcType="VARCHAR" />
    </resultMap>
    <insert id="insert" parameterType="String">
        insert into repo_issue (repo_name, member_name, issue_content, issue_label)
        values (#{repo_name,jdbcType=VARCHAR}, #{member_name,jdbcType=VARCHAR},
                #{issue_content,jdbcType=VARCHAR}, #{issue_label,jdbcType=VARCHAR})
    </insert>
    <select id="searchRepo" parameterType="String" resultMap="IssueResultMap">
        SELECT *
        FROM repo_issue
        WHERE repo_name = #{repo_name,jdbcType=VARCHAR}
        ORDER BY id DESC
    </select>
    <select id="searchMem" parameterType="String" resultMap="IssueResultMap">
        SELECT *
        FROM repo_issue
        WHERE member_name = #{member_name,jdbcType=VARCHAR}
        ORDER BY id DESC
    </select>
</mapper>